plugins {
     id 'com.bmuschko.docker-remote-api' version '4.8.0'
}

import com.bmuschko.gradle.docker.tasks.image.*

task(buildDockerImage, type: DockerBuildImage) {
    inputDir = file('.')
    tags.add('snakebot-client-dotnet')
}